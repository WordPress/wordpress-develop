<?php
/**
 * Deliberately empty replacement for the importer-controlled version include.
 *
 * Pull-request Core PHP is mounted separately for text-only source caching. The
 * parser record for wp-includes/version.php points here so the importer never
 * executes that pull-request file.
 */
